import React from 'react';

const HeroSection = () => {
  return (
    <section>
      {/* Contenido de la sección principal (Hero) */}
      <h2>Bienvenido a Barbería XYZ</h2>
      <p>Tu lugar para un corte de cabello y afeitado perfecto.</p>
      {/* Botón de llamada a la acción, imagen, etc. */}
    </section>
  );
};

export default HeroSection; 